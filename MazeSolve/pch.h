#pragma once

#include <iostream>
#include <fstream>
#include <map>
#include <vector>

using std::wcout, std::cin, std::ios, std::endl;
using std::ifstream;
using std::map, std::pair, std::make_pair;
using std::vector;